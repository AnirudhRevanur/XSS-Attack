# Lab Assignment 1

This is a Docker setup that runs a simple web application that behaves as a post broadcast application that uses ExpressJS, Postgres and nginx connected to each other through Docker Containers and Docker compose, making it reproducible and immutable